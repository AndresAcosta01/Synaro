const HeaderTitle = ({ title }) => {
    return (
        <div>
            <h1 className="text-[20px] font-display font-semibold text-ink mr-[20px]">
                {title}
            </h1>
        </div>
    );
};

export default HeaderTitle;