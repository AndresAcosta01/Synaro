const HeaderTitle = ({ title }) => {
    return (
        <div className="ml-[24px]">
            <h1 className="text-[36px] font-display font-semibold text-ink">
                {title}
            </h1>
        </div>
    );
};

export default HeaderTitle;